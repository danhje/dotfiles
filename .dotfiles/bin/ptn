#!/usr/bin/env bash

if [[ "$1" == "--version" || "$1" == "-V" ]]; then
    echo "Python $(uv python find --show-version)"
    exit 0
fi

# Fallback if something actually expects a real python instance
exec "$(uv python find)" "$@"
